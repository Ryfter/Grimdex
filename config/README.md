# config — tool-specific configuration backups

Harness/tool config snapshots (e.g. a Claude Code `settings.json`) live here, isolated
so the knowledge itself stays tool-neutral. Ships empty: your instance fills it. Keep
machine-specific values out of any repo you ever intend to share.
